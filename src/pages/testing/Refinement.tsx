import React from 'react';
import { RefinementDashboard } from '../../components/testing/Refinement';

const RefinementPage: React.FC = () => {
  return <RefinementDashboard />;
};

export default RefinementPage;